// Code generated by entc, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"go-api-report2/ent/limitpoint"

	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// LimitpointCreate is the builder for creating a Limitpoint entity.
type LimitpointCreate struct {
	config
	mutation *LimitpointMutation
	hooks    []Hook
}

// SetLimitPoint sets the "LimitPoint" field.
func (lc *LimitpointCreate) SetLimitPoint(i int) *LimitpointCreate {
	lc.mutation.SetLimitPoint(i)
	return lc
}

// SetNillableLimitPoint sets the "LimitPoint" field if the given value is not nil.
func (lc *LimitpointCreate) SetNillableLimitPoint(i *int) *LimitpointCreate {
	if i != nil {
		lc.SetLimitPoint(*i)
	}
	return lc
}

// SetID sets the "id" field.
func (lc *LimitpointCreate) SetID(i int) *LimitpointCreate {
	lc.mutation.SetID(i)
	return lc
}

// Mutation returns the LimitpointMutation object of the builder.
func (lc *LimitpointCreate) Mutation() *LimitpointMutation {
	return lc.mutation
}

// Save creates the Limitpoint in the database.
func (lc *LimitpointCreate) Save(ctx context.Context) (*Limitpoint, error) {
	var (
		err  error
		node *Limitpoint
	)
	if len(lc.hooks) == 0 {
		if err = lc.check(); err != nil {
			return nil, err
		}
		node, err = lc.sqlSave(ctx)
	} else {
		var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
			mutation, ok := m.(*LimitpointMutation)
			if !ok {
				return nil, fmt.Errorf("unexpected mutation type %T", m)
			}
			if err = lc.check(); err != nil {
				return nil, err
			}
			lc.mutation = mutation
			node, err = lc.sqlSave(ctx)
			mutation.done = true
			return node, err
		})
		for i := len(lc.hooks) - 1; i >= 0; i-- {
			mut = lc.hooks[i](mut)
		}
		if _, err := mut.Mutate(ctx, lc.mutation); err != nil {
			return nil, err
		}
	}
	return node, err
}

// SaveX calls Save and panics if Save returns an error.
func (lc *LimitpointCreate) SaveX(ctx context.Context) *Limitpoint {
	v, err := lc.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// check runs all checks and user-defined validators on the builder.
func (lc *LimitpointCreate) check() error {
	return nil
}

func (lc *LimitpointCreate) sqlSave(ctx context.Context) (*Limitpoint, error) {
	_node, _spec := lc.createSpec()
	if err := sqlgraph.CreateNode(ctx, lc.driver, _spec); err != nil {
		if cerr, ok := isSQLConstraintError(err); ok {
			err = cerr
		}
		return nil, err
	}
	if _node.ID == 0 {
		id := _spec.ID.Value.(int64)
		_node.ID = int(id)
	}
	return _node, nil
}

func (lc *LimitpointCreate) createSpec() (*Limitpoint, *sqlgraph.CreateSpec) {
	var (
		_node = &Limitpoint{config: lc.config}
		_spec = &sqlgraph.CreateSpec{
			Table: limitpoint.Table,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeInt,
				Column: limitpoint.FieldID,
			},
		}
	)
	if id, ok := lc.mutation.ID(); ok {
		_node.ID = id
		_spec.ID.Value = id
	}
	if value, ok := lc.mutation.LimitPoint(); ok {
		_spec.Fields = append(_spec.Fields, &sqlgraph.FieldSpec{
			Type:   field.TypeInt,
			Value:  value,
			Column: limitpoint.FieldLimitPoint,
		})
		_node.LimitPoint = value
	}
	return _node, _spec
}

// LimitpointCreateBulk is the builder for creating many Limitpoint entities in bulk.
type LimitpointCreateBulk struct {
	config
	builders []*LimitpointCreate
}

// Save creates the Limitpoint entities in the database.
func (lcb *LimitpointCreateBulk) Save(ctx context.Context) ([]*Limitpoint, error) {
	specs := make([]*sqlgraph.CreateSpec, len(lcb.builders))
	nodes := make([]*Limitpoint, len(lcb.builders))
	mutators := make([]Mutator, len(lcb.builders))
	for i := range lcb.builders {
		func(i int, root context.Context) {
			builder := lcb.builders[i]
			var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
				mutation, ok := m.(*LimitpointMutation)
				if !ok {
					return nil, fmt.Errorf("unexpected mutation type %T", m)
				}
				if err := builder.check(); err != nil {
					return nil, err
				}
				builder.mutation = mutation
				nodes[i], specs[i] = builder.createSpec()
				var err error
				if i < len(mutators)-1 {
					_, err = mutators[i+1].Mutate(root, lcb.builders[i+1].mutation)
				} else {
					// Invoke the actual operation on the latest mutation in the chain.
					if err = sqlgraph.BatchCreate(ctx, lcb.driver, &sqlgraph.BatchCreateSpec{Nodes: specs}); err != nil {
						if cerr, ok := isSQLConstraintError(err); ok {
							err = cerr
						}
					}
				}
				mutation.done = true
				if err != nil {
					return nil, err
				}
				if nodes[i].ID == 0 {
					id := specs[i].ID.Value.(int64)
					nodes[i].ID = int(id)
				}
				return nodes[i], nil
			})
			for i := len(builder.hooks) - 1; i >= 0; i-- {
				mut = builder.hooks[i](mut)
			}
			mutators[i] = mut
		}(i, ctx)
	}
	if len(mutators) > 0 {
		if _, err := mutators[0].Mutate(ctx, lcb.builders[0].mutation); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

// SaveX is like Save, but panics if an error occurs.
func (lcb *LimitpointCreateBulk) SaveX(ctx context.Context) []*Limitpoint {
	v, err := lcb.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}
