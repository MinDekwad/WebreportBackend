// Code generated by entc, DO NOT EDIT.

package ent

import (
	"fmt"
	"go-api-report2/ent/fileinsert"
	"strings"
	"time"

	"entgo.io/ent/dialect/sql"
)

// Fileinsert is the model entity for the Fileinsert schema.
type Fileinsert struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// Name holds the value of the "name" field.
	Name *string `json:"name,omitempty"`
	// Importdate holds the value of the "importdate" field.
	Importdate *time.Time `json:"importdate,omitempty"`
	// IsSuccess holds the value of the "IsSuccess" field.
	IsSuccess bool `json:"IsSuccess,omitempty"`
	// UpdateDate holds the value of the "UpdateDate" field.
	UpdateDate *time.Time `json:"UpdateDate,omitempty"`
}

// scanValues returns the types for scanning values from sql.Rows.
func (*Fileinsert) scanValues(columns []string) ([]interface{}, error) {
	values := make([]interface{}, len(columns))
	for i := range columns {
		switch columns[i] {
		case fileinsert.FieldIsSuccess:
			values[i] = &sql.NullBool{}
		case fileinsert.FieldID:
			values[i] = &sql.NullInt64{}
		case fileinsert.FieldName:
			values[i] = &sql.NullString{}
		case fileinsert.FieldImportdate, fileinsert.FieldUpdateDate:
			values[i] = &sql.NullTime{}
		default:
			return nil, fmt.Errorf("unexpected column %q for type Fileinsert", columns[i])
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the Fileinsert fields.
func (f *Fileinsert) assignValues(columns []string, values []interface{}) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case fileinsert.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			f.ID = int(value.Int64)
		case fileinsert.FieldName:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field name", values[i])
			} else if value.Valid {
				f.Name = new(string)
				*f.Name = value.String
			}
		case fileinsert.FieldImportdate:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field importdate", values[i])
			} else if value.Valid {
				f.Importdate = new(time.Time)
				*f.Importdate = value.Time
			}
		case fileinsert.FieldIsSuccess:
			if value, ok := values[i].(*sql.NullBool); !ok {
				return fmt.Errorf("unexpected type %T for field IsSuccess", values[i])
			} else if value.Valid {
				f.IsSuccess = value.Bool
			}
		case fileinsert.FieldUpdateDate:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field UpdateDate", values[i])
			} else if value.Valid {
				f.UpdateDate = new(time.Time)
				*f.UpdateDate = value.Time
			}
		}
	}
	return nil
}

// Update returns a builder for updating this Fileinsert.
// Note that you need to call Fileinsert.Unwrap() before calling this method if this Fileinsert
// was returned from a transaction, and the transaction was committed or rolled back.
func (f *Fileinsert) Update() *FileinsertUpdateOne {
	return (&FileinsertClient{config: f.config}).UpdateOne(f)
}

// Unwrap unwraps the Fileinsert entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (f *Fileinsert) Unwrap() *Fileinsert {
	tx, ok := f.config.driver.(*txDriver)
	if !ok {
		panic("ent: Fileinsert is not a transactional entity")
	}
	f.config.driver = tx.drv
	return f
}

// String implements the fmt.Stringer.
func (f *Fileinsert) String() string {
	var builder strings.Builder
	builder.WriteString("Fileinsert(")
	builder.WriteString(fmt.Sprintf("id=%v", f.ID))
	if v := f.Name; v != nil {
		builder.WriteString(", name=")
		builder.WriteString(*v)
	}
	if v := f.Importdate; v != nil {
		builder.WriteString(", importdate=")
		builder.WriteString(v.Format(time.ANSIC))
	}
	builder.WriteString(", IsSuccess=")
	builder.WriteString(fmt.Sprintf("%v", f.IsSuccess))
	if v := f.UpdateDate; v != nil {
		builder.WriteString(", UpdateDate=")
		builder.WriteString(v.Format(time.ANSIC))
	}
	builder.WriteByte(')')
	return builder.String()
}

// Fileinserts is a parsable slice of Fileinsert.
type Fileinserts []*Fileinsert

func (f Fileinserts) config(cfg config) {
	for _i := range f {
		f[_i].config = cfg
	}
}
