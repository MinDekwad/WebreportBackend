// Code generated by entc, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"go-api-report2/ent/bankdetail"
	"go-api-report2/ent/statementendingbalanc"

	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// BankdetailCreate is the builder for creating a Bankdetail entity.
type BankdetailCreate struct {
	config
	mutation *BankdetailMutation
	hooks    []Hook
}

// SetBankAccountNo sets the "Bank_AccountNo" field.
func (bc *BankdetailCreate) SetBankAccountNo(s string) *BankdetailCreate {
	bc.mutation.SetBankAccountNo(s)
	return bc
}

// SetNillableBankAccountNo sets the "Bank_AccountNo" field if the given value is not nil.
func (bc *BankdetailCreate) SetNillableBankAccountNo(s *string) *BankdetailCreate {
	if s != nil {
		bc.SetBankAccountNo(*s)
	}
	return bc
}

// SetBankName sets the "Bank_Name" field.
func (bc *BankdetailCreate) SetBankName(s string) *BankdetailCreate {
	bc.mutation.SetBankName(s)
	return bc
}

// SetNillableBankName sets the "Bank_Name" field if the given value is not nil.
func (bc *BankdetailCreate) SetNillableBankName(s *string) *BankdetailCreate {
	if s != nil {
		bc.SetBankName(*s)
	}
	return bc
}

// SetBankAccountName sets the "Bank_AccountName" field.
func (bc *BankdetailCreate) SetBankAccountName(s string) *BankdetailCreate {
	bc.mutation.SetBankAccountName(s)
	return bc
}

// SetNillableBankAccountName sets the "Bank_AccountName" field if the given value is not nil.
func (bc *BankdetailCreate) SetNillableBankAccountName(s *string) *BankdetailCreate {
	if s != nil {
		bc.SetBankAccountName(*s)
	}
	return bc
}

// SetID sets the "id" field.
func (bc *BankdetailCreate) SetID(i int) *BankdetailCreate {
	bc.mutation.SetID(i)
	return bc
}

// AddStatementIDs adds the "statements" edge to the StatementEndingBalanc entity by IDs.
func (bc *BankdetailCreate) AddStatementIDs(ids ...int) *BankdetailCreate {
	bc.mutation.AddStatementIDs(ids...)
	return bc
}

// AddStatements adds the "statements" edges to the StatementEndingBalanc entity.
func (bc *BankdetailCreate) AddStatements(s ...*StatementEndingBalanc) *BankdetailCreate {
	ids := make([]int, len(s))
	for i := range s {
		ids[i] = s[i].ID
	}
	return bc.AddStatementIDs(ids...)
}

// Mutation returns the BankdetailMutation object of the builder.
func (bc *BankdetailCreate) Mutation() *BankdetailMutation {
	return bc.mutation
}

// Save creates the Bankdetail in the database.
func (bc *BankdetailCreate) Save(ctx context.Context) (*Bankdetail, error) {
	var (
		err  error
		node *Bankdetail
	)
	if len(bc.hooks) == 0 {
		if err = bc.check(); err != nil {
			return nil, err
		}
		node, err = bc.sqlSave(ctx)
	} else {
		var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
			mutation, ok := m.(*BankdetailMutation)
			if !ok {
				return nil, fmt.Errorf("unexpected mutation type %T", m)
			}
			if err = bc.check(); err != nil {
				return nil, err
			}
			bc.mutation = mutation
			node, err = bc.sqlSave(ctx)
			mutation.done = true
			return node, err
		})
		for i := len(bc.hooks) - 1; i >= 0; i-- {
			mut = bc.hooks[i](mut)
		}
		if _, err := mut.Mutate(ctx, bc.mutation); err != nil {
			return nil, err
		}
	}
	return node, err
}

// SaveX calls Save and panics if Save returns an error.
func (bc *BankdetailCreate) SaveX(ctx context.Context) *Bankdetail {
	v, err := bc.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// check runs all checks and user-defined validators on the builder.
func (bc *BankdetailCreate) check() error {
	if v, ok := bc.mutation.BankAccountNo(); ok {
		if err := bankdetail.BankAccountNoValidator(v); err != nil {
			return &ValidationError{Name: "Bank_AccountNo", err: fmt.Errorf("ent: validator failed for field \"Bank_AccountNo\": %w", err)}
		}
	}
	if v, ok := bc.mutation.BankName(); ok {
		if err := bankdetail.BankNameValidator(v); err != nil {
			return &ValidationError{Name: "Bank_Name", err: fmt.Errorf("ent: validator failed for field \"Bank_Name\": %w", err)}
		}
	}
	if v, ok := bc.mutation.BankAccountName(); ok {
		if err := bankdetail.BankAccountNameValidator(v); err != nil {
			return &ValidationError{Name: "Bank_AccountName", err: fmt.Errorf("ent: validator failed for field \"Bank_AccountName\": %w", err)}
		}
	}
	return nil
}

func (bc *BankdetailCreate) sqlSave(ctx context.Context) (*Bankdetail, error) {
	_node, _spec := bc.createSpec()
	if err := sqlgraph.CreateNode(ctx, bc.driver, _spec); err != nil {
		if cerr, ok := isSQLConstraintError(err); ok {
			err = cerr
		}
		return nil, err
	}
	if _node.ID == 0 {
		id := _spec.ID.Value.(int64)
		_node.ID = int(id)
	}
	return _node, nil
}

func (bc *BankdetailCreate) createSpec() (*Bankdetail, *sqlgraph.CreateSpec) {
	var (
		_node = &Bankdetail{config: bc.config}
		_spec = &sqlgraph.CreateSpec{
			Table: bankdetail.Table,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeInt,
				Column: bankdetail.FieldID,
			},
		}
	)
	if id, ok := bc.mutation.ID(); ok {
		_node.ID = id
		_spec.ID.Value = id
	}
	if value, ok := bc.mutation.BankAccountNo(); ok {
		_spec.Fields = append(_spec.Fields, &sqlgraph.FieldSpec{
			Type:   field.TypeString,
			Value:  value,
			Column: bankdetail.FieldBankAccountNo,
		})
		_node.BankAccountNo = &value
	}
	if value, ok := bc.mutation.BankName(); ok {
		_spec.Fields = append(_spec.Fields, &sqlgraph.FieldSpec{
			Type:   field.TypeString,
			Value:  value,
			Column: bankdetail.FieldBankName,
		})
		_node.BankName = &value
	}
	if value, ok := bc.mutation.BankAccountName(); ok {
		_spec.Fields = append(_spec.Fields, &sqlgraph.FieldSpec{
			Type:   field.TypeString,
			Value:  value,
			Column: bankdetail.FieldBankAccountName,
		})
		_node.BankAccountName = &value
	}
	if nodes := bc.mutation.StatementsIDs(); len(nodes) > 0 {
		edge := &sqlgraph.EdgeSpec{
			Rel:     sqlgraph.O2M,
			Inverse: false,
			Table:   bankdetail.StatementsTable,
			Columns: []string{bankdetail.StatementsColumn},
			Bidi:    false,
			Target: &sqlgraph.EdgeTarget{
				IDSpec: &sqlgraph.FieldSpec{
					Type:   field.TypeInt,
					Column: statementendingbalanc.FieldID,
				},
			},
		}
		for _, k := range nodes {
			edge.Target.Nodes = append(edge.Target.Nodes, k)
		}
		_spec.Edges = append(_spec.Edges, edge)
	}
	return _node, _spec
}

// BankdetailCreateBulk is the builder for creating many Bankdetail entities in bulk.
type BankdetailCreateBulk struct {
	config
	builders []*BankdetailCreate
}

// Save creates the Bankdetail entities in the database.
func (bcb *BankdetailCreateBulk) Save(ctx context.Context) ([]*Bankdetail, error) {
	specs := make([]*sqlgraph.CreateSpec, len(bcb.builders))
	nodes := make([]*Bankdetail, len(bcb.builders))
	mutators := make([]Mutator, len(bcb.builders))
	for i := range bcb.builders {
		func(i int, root context.Context) {
			builder := bcb.builders[i]
			var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
				mutation, ok := m.(*BankdetailMutation)
				if !ok {
					return nil, fmt.Errorf("unexpected mutation type %T", m)
				}
				if err := builder.check(); err != nil {
					return nil, err
				}
				builder.mutation = mutation
				nodes[i], specs[i] = builder.createSpec()
				var err error
				if i < len(mutators)-1 {
					_, err = mutators[i+1].Mutate(root, bcb.builders[i+1].mutation)
				} else {
					// Invoke the actual operation on the latest mutation in the chain.
					if err = sqlgraph.BatchCreate(ctx, bcb.driver, &sqlgraph.BatchCreateSpec{Nodes: specs}); err != nil {
						if cerr, ok := isSQLConstraintError(err); ok {
							err = cerr
						}
					}
				}
				mutation.done = true
				if err != nil {
					return nil, err
				}
				if nodes[i].ID == 0 {
					id := specs[i].ID.Value.(int64)
					nodes[i].ID = int(id)
				}
				return nodes[i], nil
			})
			for i := len(builder.hooks) - 1; i >= 0; i-- {
				mut = builder.hooks[i](mut)
			}
			mutators[i] = mut
		}(i, ctx)
	}
	if len(mutators) > 0 {
		if _, err := mutators[0].Mutate(ctx, bcb.builders[0].mutation); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

// SaveX is like Save, but panics if an error occurs.
func (bcb *BankdetailCreateBulk) SaveX(ctx context.Context) []*Bankdetail {
	v, err := bcb.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}
